# pip install -r requirements.txt
# mentioning versions due to really frequent updates
tensorflow==2.12.0
pandas 
dvc
mlflow==2.2.2
notebook
numpy
matplotlib
seaborn
python-box==6.0.2
pyYAML          # for YAML files
tqdm
ensure==1.0.2
joblib
types-PyYAML
scipy
python-dotenv
Flask
Flask-Cors
gdown       # read data from google drive
-e .        # to read the src folder as a local package (library to ingest -> data ingestion)